<!DOCTYPE html>
<html lang="en">

<head>
    <!-- Bootstrap 4 Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css"
        integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <style>
        h3 {
            font-size: large;
        }

        .fa {
            margin-right: 0.5rem;
        }

        .cart-icon {
            color: #007bff;
        }

        .point-icon {
            color: #ffc107;
        }

        .gift-icon {
            color: #dc3545;
        }

        /* Bootstrap red */
    </style>
    <title>Shopping Cart</title>
</head>

<body>
    <div class="container mt-5">
        <h1 class="mb-4"><i class="fas fa-shopping-cart cart-icon"></i> Shopping Cart</h1>
        <% if (items.length > 0) { %>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Article</th>
                    <th>Quantité</th>
                    <th>Prix</th>
                    <th>Total</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <% items.forEach(item => { %>
                <tr>
                    <td><%= item.name %></td>
                    <td><%= item.quantity %></td>
                    <td><%= item.price %> points</td>
                    <td><%= item.price * item.quantity %> points</td>
                    <td>
                        <form action="/remove-from-cart" method="post">
                            <input type="hidden" name="giftId" value="<%= item.giftId %>">
                            <button type="submit" class="btn btn-outline-danger btn-sm">Retirer</button>
                        </form>
                    </td>
                </tr>
                <% }) %>
            </tbody>
        </table>
        <h2>Total : <%= totalPrice %> points</h2>
        <form action="/validate-cart" method="post">
            <button type="submit" class="btn btn-primary mt-3">Confirmer le Panier</button>
        </form>
        <% } else { %>
        <p>Votre panier est vide.</p>
        <% } %>
        <a href="/view-gifts" class="btn btn-outline-secondary mt-3">
            <i class="fas fa-arrow-left fa"></i> Retour aux Cadeaux
        </a>
    </div>

    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
</body>

</html>

